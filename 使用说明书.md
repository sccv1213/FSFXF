# 分身斧修复工具 — 使用说明书

修复直播平台录播中"拼屏布局"(同一路竖屏左中右各放一份 / 两路 8:9 / 四格)画面遮挡的工具。

---

## 1. 软件简介

### 1.1 解决的问题

主播把同一路竖屏视频左中右各放一份拼成 16:9(三格),或两路 8:9 拼成 16:9(两格)。
直播时画面四角/中间常有文字、礼物信息、水印等遮挡。

### 1.2 修复原理

**所有格子内容逐帧相同** → 从干净格子"同一相对位置"裁一块像素,盖到被遮挡的位置。
覆盖上去的内容与真实内容逐像素一致,整段视频都成立、肉眼无损。

- **补丁**:手动框选"脏区域(要盖住的地方)"和"干净区域(取像素的地方)",自动按同一相对位置对齐
- **复制规则**:把来源格**整格**内容复制覆盖到目标格(多目标格可多选)

### 1.3 适用场景

- 直播录播中三格/两格/四格拼屏,画面有 logo、礼物动画、时间码等固定位置遮挡
- 同一主播系列视频,布局和遮挡位置相同 → 用**模板** + **批量处理**一次搞定

---

## 2. 安装与启动

### 2.1 环境要求

- Windows + Python 3.13+
- **ffmpeg/ffprobe**:需安装并加入 PATH(可从 ffmpeg 官网下载,解压后将 `bin` 目录加入系统 PATH 环境变量)

### 2.2 安装依赖

```bash
pip install av==18.0.0 PySide6==6.11.1 numpy==2.5.2     # av:视频解码/编码;PySide6:界面框架;numpy:帧转换
```

### 2.3 启动

- **日常使用**:双击 `launcher.pyw`(无控制台黑窗)
- **调试/看报错**:`python launcher.pyw`(控制台版,错误输出在终端)

---

## 3. 界面总览

```
┌──────────────────────────────────────────────────────────┐
│ 工具栏:打开视频 | 保存工程 | 打开工程                      │
├──────────┬──────────────────────────────┬────────────────┤
│ 左栏      │ 画面(画布)                   │ 右栏(补丁页)   │
│ 视频信息   │ 传输栏(播放/音量/跳转/步进)  │  补丁表        │
│ 布局选择   │ 时间轴                       │  复制规则表    │
│ 编码参数   │                             │ (片段页第二页) │
├──────────┴──────────────────────────────┴────────────────┤
│ 底部队列:批量导入视频 | 添加当前工程 | 开始处理 | 日志…    │
└──────────────────────────────────────────────────────────┘
```

### 3.1 工具栏

| 按钮 | 功能 | 快捷键 |
|------|------|--------|
| 打开视频 | 打开一个视频文件,自动创建工程 | Ctrl+O |
| 保存工程 | 保存工程为 `.fsfx.json`(默认名 = 视频名) | Ctrl+S |
| 打开工程 | 载入之前保存的工程 | Ctrl+Shift+O |

### 3.2 左栏(视频信息面板)

- 视频编码信息、布局下拉(三格 9:16 / 两格 8:9 / 四格 / 无网格)
- 处理范围(时:分:秒:帧 四框,含"开头/结尾/当前"快捷按钮)
- 编码参数(质量模式/码率/输出目录等)

### 3.3 中间

- **画面**:显示当前帧,画补丁、拖动分界线、预览结果
- **传输栏**:播放/暂停、音量、当前时间/总时长、跳转输入、上一帧/下一帧、**分割**、模式按钮(选择/目标/源)、复制整格、预览当前帧、**倍速(0.5-5倍)**、**保存模板/应用模板**
- **时间轴**:片段带、处理范围、播放头;点击跳转

### 3.4 右栏

- **补丁页(第一页)**:补丁表(启用/补丁/来源格/目标格/锁定对齐/删除)+ 复制规则表(启用/复制/来源格/目标格/左右翻转/删除)
- **片段页(第二页)**:分段线列表,删除分段线 = 合并两侧分段

### 3.5 底部队列

批量导入视频、添加当前工程、移除选中、开始处理、取消当前、取消全部、日志。

---

## 4. 快速上手(5 分钟流程)

1. **打开视频**(Ctrl+O)
2. **选布局**:左栏布局下拉选"三格 9:16"(默认)
3. **画补丁**:点传输栏"**目标**"按钮 → 在脏区域(如左下角的水印)拖一个红色矩形
   → 自动在干净格子生成绿色源矩形;按需再画几个补丁
4. **看效果**:点"**预览当前帧**"→ 左原画、右修复后,并排对比
5. **选模式**:点"**选择**"按钮,拖动分界线/微调补丁框
6. **处理**:点"**添加当前工程**"→"**开始处理**"→ 完成弹窗 → 输出 `原名_修复.mp4`

---

## 5. 详细功能

### 5.1 布局与网格

- **切换布局**:左栏下拉(三格/两格/四格/无网格)。**布局按片段生效**——播放头在哪个片段,切换布局就作用于哪个片段(同一视频可前段三格、后段两格共存)
- **拖动分界线**:选择模式下,鼠标指向格子之间的分界线直接拖动(可处理黑边/间隙/不等宽)
- **边缘线 = 裁剪**:最左/最右格子边框,拖离原位即裁剪(画面按边缘线裁切,多消费者共享时边缘线统一生效)
- **限制**:格子最小宽 2%;有补丁/复制的片段禁止移动边界(会弹提示,需先删除该段补丁/复制)

### 5.2 画补丁(核心)

两种画法,任选其一:

- **目标模式**:先画"脏区域"(红色矩形,要盖住的地方)
  - 自动在最近的干净格子生成"源矩形"(绿色)
  - 自动选择来源格时**优先选能容纳目标宽度的格子**(格子太窄装不下会自动收窄,框体不出格)
- **源模式**:先框选"干净区域"(绿色)→ 自动映射到相邻格

画好后:

- **来源格/目标格下拉**(右栏补丁表):随时改来源格、**多选目标格**(可同时盖多个格子,不能选来源格;来源格和目标格互斥,对方已选的格子灰显)
- **锁定对齐**(默认开):源/目标矩形自动按"同一相对位置"重算(数学级对齐);解除锁定可手动拖矩形
- **微调**:选择模式下鼠标指向补丁框 → 方向键 ±1 像素微调(Shift = ±10 像素);左键直接拖动;放大后左键拖空白处平移
- **多补丁**:四角/中间有多个遮挡就画多个补丁
- **删除补丁**:右栏补丁行"删除"按钮
- **补丁序号标红**:未被任何片段启用的补丁,序号显示红色

### 5.3 复制格子(整格复制)

不需要画框:右栏"复制"页(补丁页下方)点"复制整格"(或传输栏按钮)→ 选"来源格 X → 目标格 Y(可多选)",把 X 格整格内容复制覆盖到目标格。每条规则右侧有"左右翻转"勾选框,勾选后目标格画面水平镜像。规则锚定创建时所在片段的网格,画面上来源格绿框、目标格红框高亮(常显)。

### 5.4 播放控制

| 操作 | 方式 |
|------|------|
| 播放/暂停 | 空格 或"播放"按钮(任意焦点位置全局生效,预览带声音) |
| 跳转 | 传输栏 4 个输入格(时/分/秒/帧)→ 点"跳转";帧从 1 计数 |
| 秒跳 | ←/→ = 1 秒;Ctrl+←/→ = 15 秒;Shift+←/→ = 30 秒 |
| 逐帧 | "上一帧/下一帧"按钮(点击自动暂停) |
| **倍速** | 传输栏 0.5倍/1倍/2倍/3倍/5倍 按钮,**音视频同步变速**(音调随倍速变化属预期);新打开视频默认 1 倍 |

音量条可调;播放头悬停时间轴时左下角显示时间。

### 5.5 预览当前帧

点"预览当前帧"→ 用真实渲染参数生成当前帧的修复图,**左原画右修复并排对比**。
建议跳到目标区域**未被遮挡**的时刻预览,目视确认源与目标内容一致(无差异)即可。

### 5.6 分段(布局中途变化时)

- **分割**:把播放头拖到变化点 → 点"分割"按钮(在播放头位置精确到帧添加分段线,自动跳片段页)
- 分割后:各片段独立布局/补丁勾选;**勾选补丁/复制 = 在当前时间所在片段生效**
- 不分段时:补丁/复制默认应用于**整个视频**
- **删除分段线**(片段页):删除 = 合并两侧分段(补丁/复制勾选清空、分界线重置);删除所有分段线后自动回到"全视频"模式;开头/结尾的永久分段线不可删除
- **应用补丁到多段**:逐段把播放头移到该段,勾选需要的补丁;或保存模板后逐段应用

### 5.7 模板(保存/应用)

把一套"补丁 + 复制规则 + 网格(分界线/边缘线)"保存为模板文件,一键应用到其他分段/视频。

- **保存模板**(传输栏,倍速右边):输入名称 → 保存当前时间所在分段的网格 + 该段启用的补丁/复制(无分段 = 全局)到软件目录 `templates/<名>.fstpl.json`
- **应用模板**:选模板文件 → 替换当前时间所在分段(全局模式 = 替换全局)的补丁/复制/网格(替换现有内容)
- 典型用法:一套布局修复 N 个视频 → 保存模板 → 每个视频打开后应用模板

### 5.8 批量处理视频

**前提**:当前工程**没有分段**(有分段时入口会提示"请先删除分段")。

1. 打开一个视频,配好补丁/复制/布局/编码参数(不分段)
2. 底部队列面板点"**批量导入视频**"
3. 多选视频(支持 mp4/mkv/ts/flv/mov/avi)
4. 每个视频立即入队;打不开/校验不过的自动跳过,原因见队列"日志"
5. 点"开始处理",全部跑完后弹一个汇总窗口(成功 N 个 / 失败列表)

每个视频输出到**它自己的目录**下的 `原名_修复.mp4`(补丁按归一化坐标,不同分辨率视频通用)。

### 5.9 渲染队列

| 按钮 | 功能 |
|------|------|
| 批量导入视频 | 按当前工程设置批量处理多个视频(需无分段) |
| 添加当前工程 | 把当前工程加入队列 |
| 移除选中 | 移除选中的任务(排队中/已完成可移除;运行中不可) |
| 开始处理 | 按顺序开始处理全部排队任务(取消过的任务会重置为排队,可重跑) |
| 取消当前 / 取消全部 | 中断处理(任务保留在队列,标记"已取消") |
| 日志 | 显示处理日志(编码命令、批量跳过原因等) |

**完成提醒**:全部任务处理完后统一弹一个窗口——全部成功显示"全部 N 个任务处理完成";有失败显示"完成 N 个,失败 M 个"及失败任务列表。

### 5.10 渲染参数(左栏)

| 项 | 默认 | 说明 |
|----|------|------|
| 编码器 | 硬件优先 NVENC | 同码率画质与 x264 medium 无肉眼差异,速度快 5-10 倍;**失败时弹窗三选一(软件重试/跳过/取消全部),不自动切换** |
| 视频码率 | 匹配原码率 | 体积 ≈ 原视频;可换 CRF 恒定质量(画质优先)或自定义 |
| 音频 | 流复制(零损失) | 默认不重编码;可重编码并设码率。**处理范围不是全片时**,音频会单独重编码一次以对齐视频边界(码率参照源音频) |
| 色彩 | 透传元数据 | 不偏色 |
| 核验 | 自动对比输出参数 | 分辨率/帧率/码率偏差超限会告警 |
| 输出目录 | 源视频同目录 | 可自定义;重名自动加序号 |

### 5.11 工程文件

- **保存工程**(Ctrl+S):默认文件名 = 视频名(`视频名.fsfx.json`),包含全部补丁/分段/布局/设置
- **打开工程**(Ctrl+Shift+O):恢复全部内容;视频文件移动后重开工程会提示重新定位
- 工程格式 version=3,可自动迁移旧 v2 工程;更早的 v1 旧档需重建

---

## 6. 快捷键一览

| 键 | 功能 |
|----|------|
| Ctrl+O / Ctrl+S / Ctrl+Shift+O | 打开视频 / 保存工程 / 打开工程 |
| 空格 | 播放/暂停(任意焦点位置全局生效,预览带声音) |
| ← / → | 后退 / 前进 1 秒(Ctrl = 15 秒,Shift = 30 秒)——未选中补丁且未悬停分界线时 |
| ←/→/↑/↓ | 选择模式下:指向补丁框微调 ±1 像素(Shift = ±10 像素);指向分界/边缘线移线 |
| Esc | 取消当前绘制 |
| 滚轮 | 缩放画面(放大后选择模式左键拖空白处平移) |

---

## 7. 常见问题与排查

### 7.1 打开视频失败

- 确认视频格式 ffmpeg 支持;状态栏/弹窗会显示 ffprobe 报错

### 7.2 画了补丁但预览没变化

- 检查补丁是否**在当前片段勾选**(右栏补丁表"启用"列;全局模式自动全选)
- 检查目标格是否被全取消(目标格下拉为空 = 补丁停用;重勾选即可恢复)
- 预览对比时注意:源与目标内容本身相同才无差异,跳到目标区域未被遮挡的时刻确认

### 7.3 移动边界被禁止

当前片段有补丁/复制时禁止移动边界(避免对齐错位)。删除该段的补丁/复制,或先移走播放头到无补丁的段再移动。

### 7.4 批量导入后有视频没入队

点队列面板"**日志**"按钮查看跳过原因(如文件不是有效视频)。

### 7.5 播放倍速切到更慢倍率后画面停顿

已修复(切倍率时时钟锚点自动重设);如仍异常,确认使用的是最新版本。

### 7.6 输出视频参数异常(核验告警)

分辨率/帧率/码率偏差超限会弹告警;确认编码参数(尤其 CRF 或码率设置)与源视频匹配。

### 7.7 音频异常

- 预览无声音:状态栏橙色徽标提示(无输出设备/音频初始化失败),画面不受影响
- 输出音轨:默认流复制零损失;重编码时注意码率设置

---

## 8. 输出文件说明

- 输出:`原名_修复.mp4`(源视频同目录,或左栏自定义输出目录;重名自动加序号)
- 源视频**不会被修改**
- 工程文件 `.fsfx.json`、模板文件 `templates/*.fstpl.json`
